

export enum OrderStatus {
    pending = 'pending',
    accepted = 'accepted',
    delivery = 'delivery',
    cenceled = 'cenceled',
    completed = 'completed'
}

export enum Currency {
    'uzs' = "uzs",
    'usd' = "usd"
}
